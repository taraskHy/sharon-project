"""Bounded Claude (implementer) <-> OpenAI/OpenRouter (reviewer) collaboration harness.

Developer tooling only. The autograder runtime never imports this package,
and this package must keep working when no API key / model is configured
(dry-run, mock mode). See docs/ai-collab.md.
"""

__version__ = "0.1.0"
